<UserControl x:Class="Desktop.MaskedTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Desktop"
             mc:Ignorable="d" d:DesignWidth="300" d:DesignHeight="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <DataGrid Grid.Column="0" x:Name="MaskedTableGrid"  AutoGenerateColumns="True" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" VerticalScrollBarVisibility="Auto">
            <!--<DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding .OriginalValue}"/>
                <DataGridTextColumn Header="Date" Binding="{Binding .MaskedValue}"/>
            </DataGrid.Columns>-->
        </DataGrid>
    </Grid>
</UserControl>
